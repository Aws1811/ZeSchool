# ZeSchool Client

Updated ZeSchool frontend using Design 1 as the visual direction.

## Run

```bash
npm install
npm run dev
```

Parent preview: `/parent/chat`
Teacher preview: `/teacher/chat`

The frontend uses React, React Router, Material UI, and CSS Modules. Mock data is kept in `src/data/mockData.js` until the backend is connected.
